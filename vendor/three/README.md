Three.js r180 (MIT). Vendored from the same release:

- `three.module.js` — WebGL extras (imports `./three.core.js`)
- `three.core.js` — core math, materials, InstancedMesh, …
- `addons/controls/OrbitControls.js`

Upgrade all three together. Keep `LICENSE`.
