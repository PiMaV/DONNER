socket.io-client v4.8.1 (MIT). Vendored browser ESM build:

- `socket.io.esm.min.js` — `import { io } from "..."` (Engine.IO 4 / Socket.IO 5)

Matches python-socketio 5.x / flask-socketio used by WOLKE and the EVT sidecar.
Upgrade the file and `LICENSE` together. Do not fetch from a CDN at runtime.
